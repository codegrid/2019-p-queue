{
  "name": "sandbox-p-queue",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel examples/**/index.html",
    "build": "rm -rf dist && parcel build examples/**/index.html --no-source-maps --public-url=/2019-p-queue",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "fmt": "prettier examples/**/*.js --write"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "2.1.1",
    "parcel-bundler": "1.12.4",
    "prettier": "1.19.1"
  },
  "dependencies": {
    "jquery": "3.4.1",
    "mobx": "5.15.1",
    "p-queue": "6.2.1"
  },
  "browserslist": [
    "last 2 Chrome version",
    "last 2 Firefox version",
    "last 2 Edge version",
    "last 2 Safari version"
  ]
}
